# Phase 10: Context Graph

## Graph Structure
Built natively into `SecurityContextGraphDto`, containing `nodes` (`SecurityContextNodeDto`) and `edges` (`SecurityContextEdgeDto`). 
The frontend can render this via mapping the Nodes to visual elements and Edges as connections without relying on complex external graph DB logic like Neo4j.
