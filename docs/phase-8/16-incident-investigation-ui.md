# Phase 8: Incident Investigation UI

## Execution
- Built `/incidents` (list view) and `/incidents/:id` (detail view).
- Details view illustrates risk visually using a dynamic progress meter scaling to the backend's explicit 0-100 risk score bounds.
- Included an AI explanation block stating explicitly that findings are grounded in deterministic engine rules rather than hallucinated responses.
