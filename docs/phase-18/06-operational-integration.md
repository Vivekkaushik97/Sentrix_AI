# Phase 18: Operational Integration

## Overview
Connected threat hunts back into the core operational workflow (Incidents/Investigations).

## Action Proposals
- A finding can serve as the justification for a `PROPOSED` Security Action (e.g., Blocking an IP).
- **CRITICAL BOUNDARY:** This action MUST go through the Phase 11 `ApprovalGate`. Sentrix AI does not support autonomous remediation. AI cannot bypass or auto-approve the generated proposal.
